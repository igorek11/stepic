CONFIG = {
    # 'mode': 'wsgi',
    'working_dir': '/etc/box/web',
    # 'python': '/usr/bin/python',
    'args': (
        '--bind=0.0.0.1:8080',
        '--workers=16',
        '--timeout=60',
        'helo.app',
    ),
}